import UpAddressSelector from './src/xt.address.selector.vue';


UpAddressSelector.install = function(Vue) {
    Vue.component(UpAddressSelector.name, UpAddressSelector);
};

export default UpAddressSelector;
